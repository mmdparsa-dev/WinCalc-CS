<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="using:GraphControl">
    <Style TargetType="local:Grapher">
        <Setter Property="Background" Value="#181818"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:Grapher">
                    <Grid x:Name="RootGrid" Background="{TemplateBinding Background}">
                        <Canvas x:Name="GraphCanvas" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                        <SwapChainPanel x:Name="GraphSurface" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Opacity="0" IsHitTestVisible="False"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
